#include <iostream>
#include <vector>

#define rep(i, n) for(int i = 0;i < (n);++i)

int main(int argc, char** argv) {
  int N, K;
  std::cin >> N >> K;
  std::vector<ll> a(N, 0);
  rep(i, N) std::cin >> a[i];
  std::vector<ll> win(K);
}
