#include <iostream>
#include <vector>
#include <cmath>

using ll = long long;
#define rep(i,n) for(int i = 0;i < (n);++i)

int main() {
  int N;
  std::cin >> N;
  std::cout << std::pow(N, 3) << '\n';
}
